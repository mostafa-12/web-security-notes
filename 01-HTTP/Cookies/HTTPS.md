## HTTPS

- HTTP over SSL/TLS.
- Uses encrypted transport instead of plain TCP.
- Provides:
	- Confidentiality (Encryption)
	- Integrity (Prevent data modification)
- HTTP Requests and Responses remain exactly the same; only the transport layer is encrypted.
- SSL is deprecated and replaced by TLS, but the term "SSL" is still commonly used.
