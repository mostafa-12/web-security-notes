## HTTP Protocol 
	Hyper Text Transfare Protocol 
	it is the core of the Web today 
- massage-based model (request and response between client and server)
- stateless protocol (not remember any thing from previous request)
- using stateful TCP protocol (all data must arrives completely and checked)